#include <stdio.h>

/**
* main - entry point
* Discription-cheking if number is positive
* Return:  always  #
*/

int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; ++a)
{
		printf("%c", a);
}
	return (0);
}
