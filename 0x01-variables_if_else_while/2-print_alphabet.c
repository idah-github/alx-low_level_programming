#include <stdio.h>

/**
* main - entry point
* Discription-cheking if number is positive
* Return:  always  #
*/

int main(void)
{
	
	for (int alph = 97; alph <= 122; alph++);
	{
	putchar(alph);
	}
	putchar('\n');
	return (0);	
}
