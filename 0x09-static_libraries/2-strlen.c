#include "main.h"
#include <stdio.h>
#include <string.h>

/**
*_strlen - checks the string length
*@s: the str var
*Return: return the str len
*
*/
int _strlen(char *s)
{
	int l;

	l = strlen(s);
	return (l);
}
